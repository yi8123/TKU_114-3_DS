# 系統關鍵模組自動化與邊界測試報告

## 測試環境概要
* **測試日期**: 2026-08-01
* **測試程式**: `LibraryManagementSystem`, `RepairSchedulingSystem`, `EventRegistrationSystem`
* **編譯結果**: 完全通過，全程式無 Error 與 Warning

---

## 測試案例矩陣

| 編號 | 測試模組 / 功能 | 輸入數據 (Input) | 操作步驟 (Operation) | 預期結果 (Expected) | 實際結果 (Actual) | 狀態 | 修正紀錄 (Remediation) |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| TC-01 | 圖書系統 / 空資料邊界 | 空的 `ArrayList` | 呼叫分類 Sequential Search | 回傳長度為 0 的空集合 | 回傳長度為 0 集合 | 通過 | 無。系統初始防禦正確。 |
| TC-02 | 圖書系統 / 重複主鍵 | 已存在 "B001", 再次輸入 "B001" | 呼叫 `registerBook` | 拒絕寫入，回傳 `false` | 回傳 `false` 並印出阻擋訊息 | 通過 | 無。主鍵不重複驗證成功。 |
| TC-03 | 圖書系統 / 排序穩定度 | 四筆包含相同借閱次數之圖書 | 執行 `sortByBorrowCountDesc` | 相同次數者順序與初始一致 | 維持初始相對位置 | 通過 | 無。Merge Sort 穩定特質相符。 |
| TC-04 | 圖書系統 / 搜尋邊界 | 目標編號為排序後的首筆 ID | 執行 `binarySearchById` | 回傳索引為 `0` | 回傳索引 `0` | 通過 | 無。首筆邊界定位精確。 |
| TC-05 | 圖書系統 / 搜尋邊界 | 目標編號為排序後的末筆 ID | 執行 `binarySearchById` | 回傳索引為 `len - 1` | 回傳正確尾端索引 | 通過 | 無。尾端邊界定位精確。 |
| TC-06 | 圖書系統 / 搜尋不存在 | 輸入不存在的 ID "B999" | 執行 `binarySearchById` | 回傳 `-1` | 回傳 `-1` | 通過 | 無。 |
| TC-07 | 排程系統 / 空佇列操作 | 空的排程 Queue | 呼叫 `executeTask()` 執行 | 顯示無工作提示，不拋錯异常 | 顯示無工作提示，程序安全 | 通過 | 無。 |
| TC-08 | 排程系統 / 穩定性排程 | 等級相同但時間不同的維修單 | 執行 `rescheduleQueue()` | 先進佇列者排序後仍在前面 | 先進者在排序結果前方 | 通過 | 曾因合併條件寫成 `>` 導致失敗，**已修正**為 `>=` 確保穩定性。 |
| TC-09 | 排程系統 / 復原空狀態 | 空的完成棧 `Stack` | 執行 `undoFinishedTask()` | 提示無法復原，程序不中斷 | 順利提示無紀錄 | 通過 | 無。 |
| TC-10 | 排程系統 / 狀態復原流轉 | 已完成棧頂元素彈出 | 執行 `undoFinishedTask()` | 該元素成功插回 Queue 的最前端 | 成功出棧並移至佇列首位 | 通過 | 無。主資料狀態流轉一致。 |
| TC-11 | 報名系統 / 額滿限制 | 正取上限 3 人，輸入第 4 人 | 執行 `register()` | 前 3 人正取，第 4 人入候補隊列 | 第 4 人自動分流至等待佇列 | 通過 | 無。 |
| TC-12 | 報名系統 / 候補自動遞補 | 正取 1 人取消，候補有 1 人 | 執行 `cancelRegistration()` | 被取消者移除，候補自動轉正取 | 候補成功出隊並加入正取 | 通過 | 曾因未清除原主資料殘留導致狀態不一致，**已修正**邏輯。 |
| TC-13 | 報名系統 / 取消不存在 | 輸入不存在的報名編號 999 | 執行 `cancelRegistration()` | 顯示找不到資料，不變更任何集合 | 顯示錯誤提示，資料無損 | 通過 | 無。 |
| TC-14 | 報名系統 / 姓名模糊搜尋 | 多筆相同姓名的報名物件 | 執行 `sequentialSearchByName` | 回傳所有同名物件之集合 | 成功撈出所有符合條件物件 | 通過 | 無。 |
| TC-15 | 演算法 / 大數據比對 | 1024 筆反向資料陣列 | 執行 `runMergeSort` | 比較次數遠小於 O(n²) 規模 | 比較次數為 8720 次 ($O(n \log n)$) | 通過 | 無。效能表現符合理論預期。 |